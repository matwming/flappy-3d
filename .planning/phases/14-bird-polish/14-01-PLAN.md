---
phase: 14
plan: 01
type: auto
autonomous: true
---

# Phase 14 Plan 01: Bird Polish

## Objective

Add rim-light shader extension to the bird's toon material and animated wing meshes that flap on each jump — completing POLISH-01 and POLISH-02 from REQUIREMENTS.md.

## Context

- Phase 13 (day/night cycle) shipped. Bundle 198.90KB.
- Bird is currently a sphere with MeshToonMaterial. No wings, no edge lighting.
- GSAP is already in use for squashStretch (anim.ts).
- prefersReducedMotion gate pattern already established in main.ts.

## Tasks

### Task 1: Rim-light extension on toon material (POLISH-01)

**type: auto**

Extend `src/render/toonMaterial.ts`:
- Add `addRimLight(material, rimStrength)` helper that hooks `onBeforeCompile`
- Injects `uRimStrength` uniform and rim-light fragment shader contribution
- Uses `vViewPosition` (available in MeshToonMaterial) and `normal` (resolved `vNormal` in fragment)
- Apply to bird material in main.ts after creation; pipes do NOT get rim-light

Commit: `feat(14-01): rim-light shader extension on bird toon material (POLISH-01)`

### Task 2: Wing meshes + animation (POLISH-02)

**type: auto**

- `src/entities/Bird.ts`: add `leftWing` and `rightWing` as public `Mesh` refs, BoxGeometry children of `bird.mesh`
- `src/anim/anim.ts`: add `wingFlap(bird: Bird)` GSAP timeline helper
- `src/main.ts`: wire `wingFlap(bird)` in `input.onFlap` playing state, gated by `prefersReducedMotion`

Commit: `feat(14-01): bird wing meshes + GSAP wingFlap animation (POLISH-02)`

### Task 3: SUMMARY + state update

**type: auto**

Write SUMMARY.md, update ROADMAP.md and STATE.md.

Commit: `docs(14-01): complete Bird Polish plan — SUMMARY + STATE/ROADMAP updated`

## Verification

- tsc --noEmit exits 0 after each task
- `onBeforeCompile` present in toonMaterial.ts
- `uRimStrength` uniform injected
- `leftWing` and `rightWing` defined in Bird.ts
- `wingFlap` defined in anim.ts and called in main.ts
- `wingFlap` call gated by `prefersReducedMotion(storage)`
- Bundle ≤250KB after build

## Success Criteria

- [ ] POLISH-01: Rim-light shader on bird material (not pipes)
- [ ] POLISH-02: Wing meshes animate on flap, motion-gated
- [ ] tsc clean; bundle ≤250KB
- [ ] No regression on Phase 12 clouds or Phase 13 sky cycle

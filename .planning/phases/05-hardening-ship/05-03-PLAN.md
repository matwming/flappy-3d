---
phase: 05-hardening-ship
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
files_modified:
  - README.md
  - src/ui/screens/SettingsModal.tsx
  - src/ui/styles.css
  - docs/SHIPPED.md
autonomous: false
requirements:
  - VIS-07
  - PERF-03

must_haves:
  truths:
    - "README contains a 'Hardening verification' section with step-by-step test procedures for all 4 SC checks"
    - "README Live link points to the actual deployed URL (not <owner> placeholder)"
    - "SettingsModal Music toggle has an iOS silent switch note visible to users"
    - "v1.0.0 tag exists in git"
    - "60fps on real device confirmed (PERF-03 carry-forward closed)"
    - "Parallax background renders smoothly on device (VIS-07 confirmed)"
    - "Tab-blur pauses music; returning to tab shows paused state (SC-3 confirmed)"
    - "iOS audio plays after first tap with ringer ON (SC-2 confirmed)"
  artifacts:
    - path: "README.md"
      provides: "Hardening verification section with SC-1..SC-4 test procedures"
      contains: "Hardening verification"
    - path: "src/ui/screens/SettingsModal.tsx"
      provides: "iOS silent switch tooltip under Music toggle"
      contains: "silent switch"
    - path: "docs/SHIPPED.md"
      provides: "v1.0.0 ship summary with requirements coverage table"
      contains: "v1.0.0"
  key_links:
    - from: "README.md Hardening verification section"
      to: "Phase 5 SC-1..SC-4 test procedures"
      via: "human-readable checklist"
      pattern: "Hardening verification"
    - from: "git tag v1.0.0"
      to: "shipped artifact"
      via: "git tag + push origin"
      pattern: "v1.0.0"
---

<objective>
Finalize documentation, add the iOS silent switch note to SettingsModal, run real-device verification of all 4 Phase 5 Success Criteria, and tag v1.0.0.

This plan cannot be fully autonomous — SC-2 (iOS audio on real device), SC-3 (tab-blur), and PERF-03 (60fps) require a human with a real iOS/Android device. The automated tasks (README polish, SettingsModal note, docs/SHIPPED.md, git tag) can run autonomously; the human verification tasks pause for user confirmation.

Purpose: Complete the "Looks Done But Isn't" checklist from PITFALLS.md. The v1.0.0 tag is the symbolic completion marker for the project.
Output: Updated README, SettingsModal with iOS note, docs/SHIPPED.md summary, git tag v1.0.0.
</objective>

<execution_context>
@/Users/ming/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ming/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ming/projects/flappy-3d/.planning/PROJECT.md
@/Users/ming/projects/flappy-3d/.planning/ROADMAP.md
@/Users/ming/projects/flappy-3d/.planning/STATE.md
@/Users/ming/projects/flappy-3d/.planning/phases/05-hardening-ship/05-01-SUMMARY.md
@/Users/ming/projects/flappy-3d/.planning/phases/05-hardening-ship/05-02-SUMMARY.md
</context>

<interfaces>
<!-- Key decisions from 05-CONTEXT.md -->
<!-- D-04: iOS verification test plan documented in README -->
<!-- D-05: iOS silent switch note in SettingsModal under Music toggle -->
<!-- D-06: Tab-blur verification procedure -->
<!-- D-09: SC-4 stopped-actor warnings test -->
<!-- D-11: listener accumulation test -->
<!-- D-17: 60fps confirmation procedure (PERF-03 carryover) -->
<!-- D-18: Parallax background visual verification on device -->
<!-- D-19: git tag v1.0.0 is last task -->
<!-- D-20: docs/SHIPPED.md — Claude's discretion -->

From README.md (current state — lines 5 and 38):
```markdown
**Live:** https://<owner>.github.io/flappy-3d/   ← needs real URL
## Performance Testing   ← procedure already exists; add Hardening section below it
```

From src/ui/screens/SettingsModal.tsx — Music toggle location:
```typescript
h(Toggle, {
  label: 'Music',
  checked: settings.music,
  onChange: (v) => update({ music: v }),
}),
```
Need to add a small note element immediately after this Toggle in the JSX.

Actual deployed URL: https://matwming.github.io/flappy-3d/
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: README polish — fix Live link + add Hardening verification section</name>
  <files>README.md</files>
  <read_first>README.md — read fully before editing (already read in planning, but re-read before patching)</read_first>
  <action>
Make two targeted edits to README.md:

**Edit 1 — Fix the Live link (line 5):**
Change:
```markdown
**Live:** https://<owner>.github.io/flappy-3d/
```
To:
```markdown
**Live:** https://matwming.github.io/flappy-3d/
```

**Edit 2 — Add "Hardening verification" section after "## Performance Testing" section:**

Insert the following section between "## Performance Testing" and "## Deployment":

```markdown
## Hardening verification

Run these checks before tagging a release. Each maps to a Phase 5 Success Criterion.

### SC-1: Memory stability (PERF-05)

1. Open https://matwming.github.io/flappy-3d/ in Chrome desktop (DEV build: `npm run dev`)
2. Open DevTools → Console
3. Play 10 death + restart cycles
4. Observe `[mem probe] round=N geometries=X textures=Y` log lines (DEV build only)
5. **Pass:** geometries and textures values plateau — no consistent growth across rounds

Alternate (any browser): DevTools → Memory → take Heap Snapshot before and after 10 rounds. `WebGLBuffer` count should not grow.

### SC-2: iOS audio — real device (AUD-01, AUD-02)

1. Open https://matwming.github.io/flappy-3d/ in Safari on a real iOS device (iOS 16+)
2. Tap the screen to start a game
3. Confirm flap, score, and death sounds play immediately (not synth oscillators)
4. In Safari Web Inspector (Mac → Develop menu → your device), run: `Howler.ctx.state` — expected: `"running"`
5. **Ringer ON:** all audio plays normally
6. **Ringer OFF (silent switch):** audio is silenced by iOS — this is expected behaviour, not a bug. The Settings modal documents this.
7. **Pass:** `Howler.ctx.state === 'running'` after first tap; SFX are recognisably flap/score/death sounds

### SC-3: Tab-blur pause/resume

1. Open the game and start a round
2. Switch to a different tab (or press Cmd+H on mobile to background the browser)
3. **Expected:** music stops immediately; game transitions to paused state
4. Return to the tab
5. **Expected:** Pause screen is shown; score is preserved
6. Tap RESUME
7. **Expected:** music resumes; game continues
8. **Pass:** All three steps confirmed correct. No music ghost-playing after tab-switch.

### SC-4: No stopped-actor warnings, no listener accumulation

1. Open https://matwming.github.io/flappy-3d/ in Chrome
2. Open DevTools Console (filter: Warnings + Errors)
3. Play 20 death + restart cycles
4. **Pass:** Zero "Event sent to stopped actor" warnings

Listener stability check (Chrome only):
1. After page load (before any play): `Object.values(getEventListeners(window)).flat().length` — record baseline
2. After 10 restart cycles: run same command
3. **Pass:** Count is identical (or within ±1 for browser internals)
```

Do NOT change any other section of README.md. Do NOT reformat existing sections.

Per D-04, D-06, D-09, D-11 from CONTEXT.md.
  </action>
  <verify>
    <automated>grep -n "Hardening verification" /Users/ming/projects/flappy-3d/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Hardening verification" README.md` returns ≥1 hit
    - `grep -n "matwming" README.md` returns ≥1 hit (real URL, not placeholder)
    - `grep -c "SC-1\|SC-2\|SC-3\|SC-4" README.md` returns ≥ 4 (one per success criterion)
    - `grep -c "Howler.ctx.state" README.md` returns ≥1 (iOS audio check)
    - `grep -n "<owner>" README.md` returns 0 (placeholder removed)
  </acceptance_criteria>
  <done>README contains real deployed URL and a complete "Hardening verification" section covering all 4 Phase 5 Success Criteria with step-by-step test procedures.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add iOS silent switch note to SettingsModal</name>
  <files>src/ui/screens/SettingsModal.tsx</files>
  <read_first>src/ui/screens/SettingsModal.tsx — read fully before editing</read_first>
  <action>
Add a small informational note immediately after the Music toggle in SettingsModal. The note should explain iOS silent switch behaviour to users who experience unexpected audio silence.

Find the Music Toggle in the JSX (around line 74-77):
```typescript
h(Toggle, {
  label: 'Music',
  checked: settings.music,
  onChange: (v) => update({ music: v }),
}),
```

Insert this element immediately after the Music Toggle closing:
```typescript
h('p', { className: 'settings-note', 'aria-live': undefined },
  'On iOS, the silent switch mutes all app audio.'
),
```

This adds a single line of explanatory text. The class `settings-note` needs a CSS rule. Add to `src/ui/styles.css`:

```css
.settings-note {
  font-size: 0.72rem;
  color: rgba(255, 255, 255, 0.55);
  margin: -0.25rem 0 0.5rem 0;
  padding: 0;
}
```

**Only touch:**
- `src/ui/screens/SettingsModal.tsx` — add the `h('p', ...)` element after the Music toggle
- `src/ui/styles.css` — add `.settings-note` rule

Do NOT modify the Toggle component. Do NOT change the Music toggle logic. Do NOT add any new imports. `h` is already imported from 'preact'.

Per D-05 from CONTEXT.md.
  </action>
  <verify>
    <automated>grep -n "silent switch" /Users/ming/projects/flappy-3d/src/ui/screens/SettingsModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "silent switch" src/ui/screens/SettingsModal.tsx` returns ≥1 hit
    - `grep -n "settings-note" src/ui/styles.css` returns ≥1 hit
    - `npx tsc --noEmit` exits 0 (no new type errors)
    - The note element uses `h('p', ...)` with a className, not raw HTML string injection
  </acceptance_criteria>
  <done>SettingsModal shows "On iOS, the silent switch mutes all app audio." below the Music toggle. Users who flip the ringer switch will understand this is platform behaviour, not a bug.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create docs/SHIPPED.md v1.0.0 summary</name>
  <files>docs/SHIPPED.md</files>
  <read_first>
    /Users/ming/projects/flappy-3d/.planning/ROADMAP.md — for requirement coverage table
    /Users/ming/projects/flappy-3d/.planning/STATE.md — for current metrics
  </read_first>
  <action>
Create `docs/SHIPPED.md` (create `docs/` directory if it doesn't exist). This is Claude's discretion per D-20 — fold the ship summary into a standalone file rather than further expanding README.

```markdown
# Flappy 3D — v1.0.0 Ship Summary

**Shipped:** 2026-04-29
**Live URL:** https://matwming.github.io/flappy-3d/
**Bundle:** 188 KB gzip (62 KB under 250 KB budget)

---

## What shipped

A polished 3D Flappy Bird-style PWA. Cel-shaded graphics (Three.js toon materials), full audio with iOS unlock (Howler.js), GSAP juice (squash-and-stretch, screen shake, particles), XState state machine, Preact DOM overlay UI, Service Worker (offline-capable, Lighthouse PWA 100%), GitHub Actions CI/CD.

---

## Requirements coverage

62/62 v1 requirements shipped across 5 phases, 17 plans, ~40+ atomic commits.

| Phase | Requirements | Status |
|-------|-------------|--------|
| 1 — Scaffold + Core Loop | HYG-01..04, CORE-01..04, VIS-02/05/06, PERF-02/06/07 | Complete |
| 2 — Game Machine + Rendering | CORE-05..08, VIS-01/03/04/07, PERF-04, SAVE-01/02 | Complete |
| 3 — UI + Audio + Polish | HUD-01..08, AUD-01..05, ANIM-01..06, SAVE-03/04 | Complete |
| 4 — PWA + A11Y + Bundle | PWA-01..05, A11Y-01..05, PERF-01/03, DEPLOY-01..03 | Complete |
| 5 — Hardening + Ship | PERF-05, VIS-07, AUD-02 (carry-forward) | Complete |

---

## Known limitations

- **Endless mode only** — time-attack, daily-seed, and hardcore modes deferred to v2.
- **Local leaderboard only** — top-5 scores in localStorage; no cloud sync.
- **iOS silent switch** — hardware ringer off silences all Web Audio (platform limitation, no API workaround).
- **Parallax wraps at x=-20** — visible seam if players watch the background for >60s without moving; negligible in active play.

---

## Stack

Vite 6 + TypeScript 5 (strict + noUncheckedIndexedAccess) + Three.js (named imports, tree-shaken) + XState v5 + Howler.js + GSAP + Preact + vite-plugin-pwa (Workbox generateSW)

---

## Acknowledgements

Reference baseline: [guiguan/flappy-anna-3d](https://github.com/guiguan/flappy-anna-3d) — the thing we benchmarked against.
```

After creating the file, confirm it exists and has content.

Per D-20 from CONTEXT.md (Claude's discretion to create docs/SHIPPED.md).
  </action>
  <verify>
    <automated>grep -n "v1.0.0" /Users/ming/projects/flappy-3d/docs/SHIPPED.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/SHIPPED.md && echo exists` returns "exists"
    - `grep -n "v1.0.0" docs/SHIPPED.md` returns ≥1 hit
    - `grep -c "Complete" docs/SHIPPED.md` returns ≥ 5 (one per phase row)
    - `grep -n "Known limitations" docs/SHIPPED.md` returns ≥1 hit
    - `wc -l docs/SHIPPED.md` returns ≥ 40 (substantive content)
  </acceptance_criteria>
  <done>docs/SHIPPED.md documents the v1.0.0 ship: live URL, requirements coverage table (62/62), known limitations, stack summary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verification: SC-2 (iOS audio), SC-3 (tab-blur), PERF-03 (60fps), VIS-07 (parallax)</name>
  <what-built>
Tasks 1-3 above (automated) have been completed:
- README updated with real URL + Hardening verification section
- SettingsModal has iOS silent switch note
- docs/SHIPPED.md created

Now the real-device tests must be run before v1.0.0 is tagged.
  </what-built>
  <how-to-verify>
Run all four checks against https://matwming.github.io/flappy-3d/

**Check A — SC-2: iOS audio (real device required)**
1. Open URL in Safari on a real iOS device (iOS 16+)
2. Tap to start a game — confirm flap/score/death sounds play
3. In Safari Web Inspector: run `Howler.ctx.state` — expected: `"running"`
4. Flip ringer to OFF — expected: audio silences (documented behaviour)
5. Settings → Music — confirm the "On iOS, the silent switch mutes all app audio." note is visible

**Check B — SC-3: Tab-blur pause**
1. Start a game round
2. Switch to a different tab
3. Return — confirm Pause screen is shown and score is preserved
4. Tap RESUME — confirm music resumes

**Check C — PERF-03: 60fps on real device**
Follow README → "Performance Testing" procedure:
1. Open on iPhone 12 or Pixel 6 in Chrome
2. Enable FPS meter via DevTools
3. Play until score ≥ 20
4. Confirm sustained ≥ 58fps

**Check D — VIS-07: Parallax background**
1. Play a round on the device
2. Observe the mountain/tree background layers
3. Confirm: mountains scroll slower than foreground, trees scroll at medium speed, no visible jitter or pop at the x=-20 wrap point during normal play

Record any failures with specific symptoms.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass. Or describe which check failed and what you observed — Claude will diagnose and fix before proceeding to tag.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Tag v1.0.0 and push</name>
  <files></files>
  <read_first>
    Run `git log --oneline -5` to confirm the current HEAD commit is the right one to tag.
    Run `git status` to confirm working tree is clean.
  </read_first>
  <action>
After human verification confirms all 4 checks pass:

```bash
# Confirm working tree is clean
git status

# Confirm TypeScript still clean
npx tsc --noEmit

# Create annotated tag
git tag -a v1.0.0 -m "v1.0.0 — Flappy 3D initial release

Phase 5 hardening complete:
- Memory stable across 10+ restart cycles (DEV probe confirms plateau)
- iOS audio verified on real device (Howler.ctx.state === 'running')
- Tab-blur pauses music and triggers paused state correctly
- No stopped-actor warnings across 20 play cycles
- Event listener count stable after 10 restarts
- Real CC0 audio samples (not synth fallback)
- 60fps confirmed on mid-tier device (PERF-03)
- Parallax background verified on device (VIS-07)

Live: https://matwming.github.io/flappy-3d/"

# Push the tag
git push origin v1.0.0
```

**Rollback path (if needed after push):** Delete the remote tag with `git push origin :refs/tags/v1.0.0` and the local tag with `git tag -d v1.0.0`. Re-run this task after fixing the issue.

This is the LAST action in Phase 5. Only execute after human verification checkpoint is passed.

Per D-19 from CONTEXT.md.
  </action>
  <verify>
    <automated>git tag --list v1.0.0</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list v1.0.0` returns "v1.0.0"
    - `git log v1.0.0 --oneline -1` shows the tagged commit message
    - `git push origin v1.0.0` exits 0 (tag is on remote)
    - `git status` shows clean working tree
  </acceptance_criteria>
  <done>v1.0.0 tag exists locally and on origin. The game is shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git tag push → origin | Annotated tag pushed to remote; rollback documented |
| iOS silent switch → user expectation | No API bypass; documented in UI to prevent user confusion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Repudiation | v1.0.0 git tag | mitigate | Annotated tag with message summarising what was verified; rollback path documented in plan |
| T-05-03-02 | Information Disclosure | docs/SHIPPED.md known limitations | accept | Documenting limitations is correct — users should know iOS silent switch behaviour |
| T-05-03-03 | Denial of Service | Premature v1.0.0 tag (human check not passed) | mitigate | Tag task (Task 5) is explicitly gated after the human verification checkpoint |
</threat_model>

<verification>
After all tasks including human verification:

```bash
cd /Users/ming/projects/flappy-3d

# 1. README has real URL and hardening section
grep -c "matwming" README.md
# Expected: ≥1

grep -c "Hardening verification" README.md
# Expected: ≥1

# 2. SettingsModal has iOS note
grep -c "silent switch" src/ui/screens/SettingsModal.tsx
# Expected: ≥1

# 3. SHIPPED.md exists
test -f docs/SHIPPED.md && wc -l docs/SHIPPED.md
# Expected: ≥40 lines

# 4. v1.0.0 tag exists
git tag --list v1.0.0
# Expected: v1.0.0

# 5. TypeScript clean
npx tsc --noEmit
# Expected: exit 0

# 6. Tag is on remote
git ls-remote origin refs/tags/v1.0.0
# Expected: non-empty (commit SHA + refs/tags/v1.0.0)
```
</verification>

<success_criteria>
- README Live link is `https://matwming.github.io/flappy-3d/` (not the `<owner>` placeholder)
- README contains "## Hardening verification" section with SC-1 through SC-4 test procedures
- SettingsModal shows "On iOS, the silent switch mutes all app audio." under the Music toggle
- docs/SHIPPED.md exists with v1.0.0 ship summary and 62/62 requirements coverage table
- Human verification: SC-2 (iOS audio), SC-3 (tab-blur), PERF-03 (60fps), VIS-07 (parallax) all confirmed PASS
- `git tag --list v1.0.0` returns "v1.0.0"; tag is pushed to origin
- `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-ship/05-03-SUMMARY.md` covering:
- Human verification results (SC-2, SC-3, PERF-03, VIS-07 pass/fail)
- README changes made (URL, new section)
- SettingsModal change summary
- docs/SHIPPED.md creation confirmation
- v1.0.0 tag SHA and push status
- Final phase 5 status: COMPLETE
</output>

---
audit_date: 2026-04-29
total_pending: 28
total_passed: 1
unique_tests_after_dedup: 24
---

# UAT Audit — Flappy 3D

Consolidated review of all human verification items across 5 phases (04, 05, 06, 07, 08). Two duplicates resolved (PERF-03 60fps appears in both Phase 4 and Phase 5). Grouped by test-session type so you can knock them out in batches.

**Live URL for all tests:** https://matwming.github.io/flappy-3d/

---

## Already PASSED (1)

- **PWA-05 / Lighthouse 1.00** — Phase 4 #1, validated by CI run 25092302358

---

## Group A — Real-device required (~10 min, iPhone 12 or Pixel 6)

These cannot be done on desktop or emulator. They gate the v1.0.0 tag.

| # | Source | Test | What to do |
|---|--------|------|-----------|
| A1 | 05 #2 | iOS Safari audio + silent switch | Open URL on iPhone Safari. Tap to start. Verify flap/score/death audible. Connect to Mac → Web Inspector → console: `Howler.ctx.state` returns `"running"`. Toggle silent switch ON → audio silenced (expected; SettingsModal note explains this) |
| A2 | 04 #2 ≡ 05 #5 | Sustained 60fps | Connect device → Chrome DevTools → Remote Devices. Performance tab → enable FPS meter (Cmd+Shift+P → "Show frames per second meter"). Play a 30s+ round with score ≥20. Verify ≥58fps sustained |
| A3 | 05 #6 | Parallax on device | Play any round on real iPhone or Android. Watch background while obstacles scroll: mountain layer slow, tree layer faster, sky stationary. No jitter at wrap point |

---

## Group B — Browser DevTools session (~15 min, Chrome on desktop)

Run from `npm run dev` (DEV mode for memory probe) or against the live deploy.

| # | Source | Test | What to do |
|---|--------|------|-----------|
| B1 | 05 #1 | Memory stability across 10 restarts (PERF-05) | `npm run dev`, open console. Play 10 deaths in a row. Watch for `[mem probe] round=N geometries=X textures=Y` lines. Both counters should plateau within 2-3 rounds and stay flat. Auto-warning fires if growth >5 after round 3 |
| B2 | 05 #3 | Tab-blur pause/resume | Start a round, score a few points. Switch tabs → music stops, state shows paused on return. Switch back → tap RESUME → music + game continue, score preserved |
| B3 | 05 #4 | No stopped-actor warnings + listener count stable | DevTools console: `_initial = getEventListeners(document); console.log(...)`. Play 20 cycles. Confirm: zero "stopped actor" warnings, getEventListeners count unchanged |

---

## Group C — Visual confirmation on live deploy (~5 min, any browser)

Open https://matwming.github.io/flappy-3d/ once and walk this list. Most items ~10s each.

### Title screen (Phase 6)
| # | Source | Test |
|---|--------|------|
| C1 | 06 #1 | Bird hovers at y=0 with smooth ~1Hz sine bob, ±0.15m amplitude. No twitching |
| C2 | 06 #2 | Pipes spawn from right, scroll past at relaxed cadence (~2.2s spawn interval), despawn off left. No collision (you can't die on title) |
| C3 | 06 #3 | Music plays at lower volume on title; jumps louder when you tap to start |
| C4 | 06 #5 | First page load: "FLAPPY 3D" letters fade in staggered (~50ms apart, ~350ms total) |
| C5 | 06 #6 | After playing a round + tapping Back to Title: logo is static (no re-animation) |
| C6 | 06 #7 | "Tap to start" CTA opacity pulses smoothly at 1.6s cycle |

### In-game juice (Phase 7)
| # | Source | Test |
|---|--------|------|
| C7 | 07 #1 | Each point scored: `+1` rises 60-80px from bird's screen position, fades over ~700ms |
| C8 | 07 #3 | At score 10, 25, 50: gold particle burst + brief screen-flash. Each fires once per round |
| C9 | 07 #4 | Consecutive pipe pairs cycle through 4 toon colors (green / teal / orange / purple) |

### Glass UI (Phase 8)
| # | Source | Test |
|---|--------|------|
| C10 | 08 #1 | `<h1>`/`<h2>` render in Press Start 2P pixel-art font; body text and buttons stay system sans |
| C11 | 08 #2 | Pause / GameOver / Settings overlays show frosted-glass blur — 3D scene blurred through them |
| C12 | 08 #3 | Buttons: subtle vertical gradient, lift on hover (desktop), press inward on click |
| C13 | 08 #4 | Tab through interactive elements: 2-layer ring (yellow inner + dark outer halo) on focus |

---

## Group D — Settings / OS toggle tests (~5 min)

| # | Source | Test |
|---|--------|------|
| D1 | 06 #4 + 07 #6 + (08 implicit) | Mac System Settings → Accessibility → Display → Reduce Motion → ON. Reload. Verify ALL motion suppressed: bird stationary, logo instant, CTA static, popups skipped, milestone bursts skipped, flap trail skipped. Pipe color cycling continues (NOT motion-related) |
| D2 | 07 #2 | Settings → toggle "Flap trail" ON → flap → 3 fading ghost echoes. Toggle OFF → no ghosts. Setting persists across page reload |
| D3 | 07 #5 + 08 #5 | Settings → toggle "Colorblind palette" ON → all pipes uniform green (no cycling), bird turns yellow, buttons turn teal-blue. Toggle OFF → revert |

---

## Group E — Ship gate (after A + B + C + D pass)

| # | Source | Action |
|---|--------|--------|
| E1 | 05 #7 | `cd /Users/ming/projects/flappy-3d && git tag -a v1.0.0 -m "v1.0.0 — Flappy 3D initial release" && git push origin v1.0.0` |

(v1.1.0 tag is your call — could ship simultaneously after Phase 6/7/8 visual UAT passes, or hold for separate sign-off.)

---

## Recommended order

1. **Group C + D in one ~10-min browser session** (knocks out 16 items)
2. **Group B in one ~15-min DevTools session** (knocks out 3 items, all on desktop)
3. **Group A in one ~10-min real-device session** (knocks out 3 items, requires iPhone)
4. **Group E** when satisfied (1 action — `git tag` + push)

Total commitment: ~40 min spread across 4 sessions. Each session is independent — you can interleave them.

---

## Failures: how to log

If anything fails during a test, the cleanest path is:
- Open the relevant `*-HUMAN-UAT.md` file
- Change `result: [pending]` to `result: FAILED — <reason>`
- Update the file's frontmatter `pending` and `issues` counters
- Run `/gsd-debug` for systematic debugging

If everything passes, batch-update each `*-HUMAN-UAT.md`:
- `result: PASSED — <date>`
- frontmatter `passed: N`, `pending: 0`, `status: complete`

---

*Audit generated 2026-04-29 by `/gsd-audit-uat` (consolidated 5 HUMAN-UAT files + 4 VERIFICATION files into single human-test plan)*
